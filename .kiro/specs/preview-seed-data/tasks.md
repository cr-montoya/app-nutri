# Tasks: Preview Seed Data

Branch: `chore/preview-seed-data`, created by `task-runner` from up-to-date `main` before T1.1, per `.agents/rules/trunk-based.md`. Depends on `infra-migration-pipeline` (merged) and `phase-1c-appointments-calendar` (its `Appointment`/`AppointmentStatus` model and exclusion constraint must exist on `main` before T1.4).

## T1: The seed script (`prisma/seed-preview.mjs`)

- [x] T1.1 Write the confirmation guard (exit 1 immediately unless `process.env.SEED_PREVIEW_CONFIRM === "1"`, before importing `PrismaClient`) and the delete-existing-seed logic from `design.md` (look up `Organization` by slug `preview-clinic`; if found, delete in the documented FK-safe order inside one `prisma.$transaction`; if not found, skip). Validation: with `SEED_PREVIEW_CONFIRM` unset, `node prisma/seed-preview.mjs` exits non-zero and makes zero database calls; with it set to `"1"` against an empty local dev database, the script reaches past the guard without error. Closes REQ-002, REQ-004.
- [x] T1.2 Add organization + the four users/memberships/professionals from `design.md`'s table, hashing the fixed password once with `@node-rs/argon2`'s `hash()`. Validation: `pnpm test -- seed-preview-users` (a small Vitest integration test that runs the script against the local dev DB, then queries for the 4 users/memberships/2 professionals by the documented emails and asserts `argon2.verify(passwordHash, "Preview1234!")` is `true` for each). Closes REQ-005, REQ-006.
- [x] T1.3 Add the 10 patients from `design.md`, patient 10 with `archivedAt` set. Validation: `pnpm test -- seed-preview-patients` asserting exactly 10 patients exist for the seed org, exactly 1 with `archivedAt` non-null, and that no seeded name/email/phone matches a real-looking pattern (test asserts every name starts with `"Test Patient"` and every phone starts with the fictional `+1555` prefix). Closes REQ-007, REQ-012 (patient portion).
- [x] T1.4 Add the 7 appointments from `design.md`'s schedule table, excluding patient 10 from any appointment. Validation: `pnpm test -- seed-preview-appointments` asserting all 5 `AppointmentStatus` values are present at least once, both professionals have at least one appointment, no seeded appointment references patient 10, and — critically — the insert itself succeeds with no exclusion-constraint violation (the test's assertion *is* running the script against a real Postgres with the constraint active, per `phase-1c-appointments-calendar`'s migration). Closes REQ-008, REQ-009.
- [x] T1.5 Reseed idempotency: run the script twice in a row against the same database. Validation: `pnpm test -- seed-preview-idempotent` running the script twice and asserting the organization, user, patient, and appointment counts for the seed org are identical after both runs (no duplication, no leftover rows from the first run). Closes REQ-004 (second half: re-run behavior).
- [x] T1.6 Logging safety and failure behavior. Validation: `pnpm test -- seed-preview-logging` capturing `stdout` and asserting it contains the slug and all 4 emails/roles, and asserts it never contains the string `"Preview1234!"`, any `passwordHash` value, or any seeded patient/appointment field value; a second case points `DATABASE_URL` at an unreachable host and asserts the script's process exit code is non-zero. Closes REQ-010, REQ-011.

## T2: Wire into the CI pipeline

- [x] T2.1 Add the `Seed preview data` step (after the existing `prisma migrate deploy` step) and the `concurrency:` block to `.github/workflows/migrate.yml`'s `migrate-preview` job only, exactly as in `design.md`. Validation: `grep -c "seed-preview" .github/workflows/migrate.yml` returns `1` (the reference appears only inside `migrate-preview`, never inside `migrate-production` — confirmed by also asserting `sed -n '/migrate-production:/,/migrate-preview:/p' .github/workflows/migrate.yml | grep -c seed-preview` returns `0`), plus a workflow syntax check (`actionlint .github/workflows/migrate.yml` if available, otherwise `gh workflow view migrate.yml` after pushing shows no parse error).
- [x] T2.2 Extend only `pull_request.paths` to include `prisma/seed-preview.mjs` and `.github/workflows/migrate.yml`, retaining `prisma/migrations/**`; leave `push.paths` migration-only. Validation: a push to PR #6 triggers exactly one `migrate-preview` run, while `migrate-production` remains skipped. Closes the live-proof trigger gap discovered during T3.1.
- [x] T2.3 Generate Prisma Client explicitly in `migrate-preview` after the hardened `pnpm install --ignore-scripts` and before the seed imports `@prisma/client`. Validation: PR #6's `migrate-preview` log shows `prisma generate` and the seed step passing. Closes the missing generated-client failure from run `33228293136`.

## T3: End-to-end proof

- [x] T3.1 Push this branch, open its PR, and let `migrate-preview` run for real against a live Neon preview branch. Validation: `gh pr checks <PR#>` shows `migrate-preview` passing; the job log (`gh run view <run-id> --log`) shows the printed slug and all 4 `email — role` lines and nothing else seed-related; manually log into the PR's Vercel preview at `/preview-clinic/appointments` as each of the 4 seeded emails (password `Preview1234!`) and confirm the calendar shows the 7 seeded appointments across both professionals with all 5 statuses visually distinguishable, and confirm `/preview-clinic/patients` shows all 10 patients with patient 10 archived. Document which PR/run this was checked against in the PR description, since this manual check isn't automatable in CI (same convention as `phase-1c-appointments-calendar`'s T4.9). Confirms REQ-001 through REQ-013 hold end to end.

## After T3.1

Run `qa` and `code-quality`, then `security` (this spec creates password hashes and a deliberately hardcoded credential — REQ-006's risk-acceptance reasoning needs an independent check, not just this document's say-so), then `reviewer` as the final gate, then `spec-closeout`, then `pr-prep`.
